{
  "name": "cmk-avrora-erp",
  "private": true,
  "version": "1.0.0",
  "description": "ЦМК АВРОРА — ERP-система планирования производства (frontend + backend monorepo)",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "db:up": "docker compose -f backend/docker-compose.yml up -d",
    "db:down": "docker compose -f backend/docker-compose.yml down",
    "db:setup": "npm --prefix backend run db:setup",
    "dev": "npm run db:up && concurrently -n BACKEND,FRONTEND -c magenta,cyan -k \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run db:setup && bash backend-go/scripts/dev.sh",
    "dev:frontend": "npm --prefix frontend run dev",
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix frontend run build",
    "build:go": "cd backend-go && go build -o bin/server ./cmd/server",
    "docker:build": "docker build -f backend-go/Dockerfile -t cmk-avrora-erp .",
    "ensure-secrets": "node scripts/ensure-secrets.mjs",
    "pilot": "node scripts/ensure-secrets.mjs && docker compose -f backend/docker-compose.yml --profile pilot up -d --build && docker image prune -f",
    "pilot:down": "docker compose -f backend/docker-compose.yml --profile pilot down",
    "pilot:logs": "docker compose -f backend/docker-compose.yml logs -f api-pilot",
    "prod:up": "node scripts/ensure-secrets.mjs && docker compose -f backend/docker-compose.yml --profile prod up -d --build && docker image prune -f",
    "prod:logs": "docker compose -f backend/docker-compose.yml logs -f api",
    "db:backup": "node scripts/db-backup.mjs",
    "import:1c": "npm --prefix backend run import:1c-csv --",
    "import:catalog": "npm --prefix backend run import:catalog --",
    "check:data": "npm --prefix backend run check:data --",
    "recalc:costing": "cd backend-go && go run ./cmd/recalc",
    "smoke": "cd backend-go && go run ./cmd/smoke"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
