# Адрес бэкенда, когда фронт и Go живут на разных доменах
# (Vercel + VPS). Пусто/не задано — тот же origin: локальная разработка
# через vite-прокси и вариант «всё в одном контейнере».
# В Vercel задаётся в Project Settings → Environment Variables.
VITE_API_URL="https://api.example.kz"
