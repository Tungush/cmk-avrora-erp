# ЦМК АВРОРА — ERP планирования производства

Веб-сервис вместо Google-таблицы на 44 листа: заказы, спецификации с трудочасами и себестоимостью, план производства, склад, финансы. Права — на уровне групп полей. Идёт переход на приём данных из 1С.

**Стек:** Go (gin + pgx) + PostgreSQL · схема и миграции — Prisma · React + Vite + Mantine · TypeScript.

> С 02.09.2026 бэкенд — `backend-go/` (полный перенос с NestJS, сверенный запрос-в-запрос). Каталог `backend/` остался за схемой БД, сидами и скриптами импорта.

---

## Быстрый старт

Нужны Docker Desktop, Node 20+ и Go 1.25 (`go` в PATH).

```bash
npm run install:all              # зависимости корня, backend (prisma), frontend
cp backend/.env.example backend/.env
npm run dev                      # Postgres в Docker + Go-бэкенд :3100 + frontend :5173
```

Бэкенд читает тот же `backend/.env` (`backend-go/scripts/dev.sh`). Вход — по реальным логинам (`AUTH_DEMO_MODE=false`); пароли выдаёт `npm --prefix backend run rotate-passwords`.

> **Node 25:** если сборка фронтенда падает на «Cannot find native binding», доустановите бинарники:
> `npm i -D @rolldown/binding-darwin-arm64 lightningcss-darwin-arm64 @tailwindcss/oxide-darwin-arm64`

---

## Наполнение данными

`npm run dev` уже накатывает схему и сид (роли, права, участки, коэффициенты). Исторические данные из Excel — отдельными командами **в этом порядке**:

```bash
cd backend

# 1. Полная копия книги: 44 листа, 103 842 строки, заказы, артикулы, заказчики
NODE_OPTIONS="--max-old-space-size=8192" npm run migrate:full

# 2. Материалы, составы изделий (BOM) и нормы труда из «Спецификации 2022»
npm run migrate:bom

# 3. Приходы металла с ценами из «База сырья (металл)»
npm run migrate:receipts

# 4. Договоры-основания из «19.20-7п»
npm run migrate:payment-docs
```

Что получится: 2 160 артикулов · 3 147 материалов · 18 141 позиция состава · 3 866 норм труда · 2 202 заказа · 4 682 ДО.

Импорты идемпотентны — повторный запуск не создаёт дублей и не затирает ручные правки инженера.

---

## Подключение к 1С

Данные принимаем **GET-запросами** к HTTP-сервисам 1С (спецификация — [docs/ТЗ_1С_запросы_GET_v10.xlsx](docs/ТЗ_1С_запросы_GET_v10.xlsx), архитектура — [08_INTEGRATION_1C.md](08_INTEGRATION_1C.md)).

```bash
# backend/.env
ONEC_BASE_URL="http://10.10.10.81"
ONEC_LOGIN="…"
ONEC_PASSWORD="…"
ONEC_TIMEZONE="Asia/Almaty"
```

Проверка и запуск синхронизации (локально бэкенд на :3100, в Docker-образе — на :3000):

```bash
# связь есть?
curl -H "Authorization: Bearer $TOKEN" \
  "http://localhost:3100/api/v1/integrations/1c/ping"

# один заказ (номер URL-кодируется, кириллица поддерживается)
curl -X POST -H "Authorization: Bearer $TOKEN" \
  "http://localhost:3100/api/v1/integrations/1c/sync/order/%D0%9F-88192-21"

# пакетом: активные заказы, начиная с самых давно не синхронизированных
curl -X POST -H "Authorization: Bearer $TOKEN" -H 'Content-Type: application/json' \
  -d '{"limit":50}' "http://localhost:3100/api/v1/integrations/1c/sync/orders"

# закуп под заказ: GET D → GET C → payment_documents
curl -X POST -H "Authorization: Bearer $TOKEN" \
  "http://localhost:3100/api/v1/integrations/1c/sync/procurement/%D0%9F-88192-21"
```

Состояние обмена — экран **«Обмен с 1С»** в меню (роль `admin` или `director`).

### Отладка без корпоративной сети

`10.10.10.81` доступен только изнутри контура. Вне его — мок тех же четырёх сервисов:

```bash
cd backend && npm run mock:1c                      # http://localhost:8081
ONEC_BASE_URL=http://localhost:8081 npm run dev     # из корня; .env не перекрывает уже заданные переменные
```

---

## Национальный каталог товаров (НКТ)

Коды NTIN для изделий ЦМК — раздел «НКТ». Периметр: только собственное
производство (`articles`, где `is_material_resale = false`). Покупная
номенклатура не входит — ей NTIN ищется по чужому GTIN, а штрихкодов у нас
нет: они живут в 1С и ни в один обмен пока не приходят.

```bash
# backend/.env — ключ из личного кабинета НКТ, раздел «Ключи API»
NKT_API_KEY="…"
NKT_BASE_URL=""            # пусто — прод; для отладки "https://stg.nct.kz/gwp"
```

Без ключа фоновые задания не поднимаются: паспорта заводятся, очередь
копится, обмена нет — как outbox без адреса 1С.

Порядок запуска:

1. «Виды и категории» — назначить ОКТРУ самым массовым видам изделий
   (`n` — 1 384 изделия, `z` — 513: это 88 % каталога).
2. «Обновить схему из НКТ» — состав характеристик задаёт каталог, руками он
   не выписывается.
3. Инженер заполняет паспорт изделия, «Сохранить и проверить» показывает,
   чего не хватает, ДО отправки — у НКТ полная валидация только на
   модерации, и каждая невалидная заявка стоит цикла модерации.
4. «Подать заявку» ставит изделие в очередь; дальше конвейер идёт сам,
   человек нужен на доработке, дублях и отказах.

Контракт API, статусы и расхождения с ТЗ — [docs/nkt/API.md](docs/nkt/API.md).

---

## Что делать дальше

### Шаг 1. Проверить связь с реальной 1С

На машине внутри корпоративной сети:

1. Прописать `ONEC_BASE_URL`, `ONEC_LOGIN`, `ONEC_PASSWORD` в `backend/.env`.
2. Дёрнуть `/integrations/1c/ping` — он возьмёт первый реальный номер заказа из базы.
3. Если ответ пришёл — сравнить **фактическую структуру JSON** с ожидаемой.

Сравнить проще всего так: выполнить в браузере ссылку из листа «Лист3» ТЗ и сопоставить с моком (`backend/tools/mock-1c-server.js`).

**Где структура угадана:** в GET A массив строк номенклатуры не имеет имени поля в ТЗ. Код перебирает известные варианты (`items`, `item_alldata`, `item_data`, `lines`, `nomenclature`) и дополнительно ищет любой массив, у элементов которого есть `item_code`. Если не нашёл — заказ **не** помечается синхронизированным, а в отчёт попадает `linesNotParsed` со списком ключей ответа: по нему сразу видно, как 1С на самом деле назвала массив. Добавить имя — в `itemArrayKeys` (`backend-go/internal/onec/sync.go`).

Сырые ответы 1С запоминаются (последний по каждому пути) — `Client.LastRaw()` в `backend-go/internal/onec/client.go`.

### Шаг 2. Синхронизировать заказы

```bash
# сначала на одном заказе, посмотреть отчёт
POST /integrations/1c/sync/order/{номер}
# затем пакетами
POST /integrations/1c/sync/orders  {"limit": 50}
```

Отчёт вернёт расхождения, которые нужно разобрать с коллегами:

| Поле отчёта | Что значит |
|---|---|
| `notFound` | Документа нет в 1С ни по одному варианту номера |
| `missingLocally` | В 1С есть, у нас нет — заказ надо завести |
| `linesNotParsed` | Ответ пришёл, но массив строк не распознан — покажет ключи JSON |
| `unparsed` | Значения, которые не удалось разобрать (в БД не записаны) |
| `unknownArticles` | Артикулы из 1С, которых нет в нашей номенклатуре |
| `unknownStatuses` | Статусы 1С без маппинга — заказ не сдвинут |

Ни одно из этих состояний **не пишет в базу мусор**: при нераспознанной структуре заказ остаётся в очереди на следующий прогон, неразобранные числа не затирают существующие значения.

### Шаг 3. Ответить на вопросы 1С-подрядчику

Полный список — в [08_INTEGRATION_1C.md §9](08_INTEGRATION_1C.md). Блокирующий:

> **Как узнать о новых заказах?** Все четыре GET-запроса точечные — по номеру документа. Списка «что изменилось с даты X» нет, поэтому заказ, заведённый в 1С, у нас сам не появится. Нужен эндпоинт, отдающий хотя бы номера заказов за период.

### Шаг 4. Отправка данных в 1С

Инфраструктура готова (outbox с гарантией доставки, HMAC, ретраи, журнал). Заявка на номенклатуру и создание заказа уже кладут события в очередь. Осталось получить от подрядчика адрес приёмника и включить `INTEGRATION_1C_URL`.

### Известные пробелы данных

| Пробел | Сколько | Что делать |
|---|---|---|
| Активные заказы без плановой даты | 247 | Заполнить или подтянуть из 1С (`workplandate`) |
| ДО без привязки к заказу | 2 660 | Связать через GET C (`clientorder_num` в строках) |
| Артикулы без состава | 462 | Заполнить в «Спецификации → Материалы» |
| Артикулы без норм труда | ~485 | Заполнить в «Спецификации → Трудозатраты» |
| Этапы производства не отмечаются | 199 заказов на первом этапе | Процесс: мастер отмечает этапы на экране «Цех» |

---

## Документация

| Файл | О чём |
|---|---|
| [07_ARCHITECTURE_AND_UX.md](07_ARCHITECTURE_AND_UX.md) | Главный план: роли, права на уровне полей, UX, дизайн-система, 6 этапов |
| [08_INTEGRATION_1C.md](08_INTEGRATION_1C.md) | Синхронизация с 1С: матрица владения данными, GET-запросы, отказ от таблиц |
| [09_COSTING_AND_STAGES.md](09_COSTING_AND_STAGES.md) | Этапы производства, себестоимость заказа, партии и подряд: почему заказ через месяц стоит других денег |
| [01_DATA_MODEL.md](01_DATA_MODEL.md) · [03_API_SPECIFICATION.md](03_API_SPECIFICATION.md) | Модель данных и API |
| [04_ROLES_PERMISSIONS.md](04_ROLES_PERMISSIONS.md) | Роли и права |
| [06_SHEET_ANALYSIS_AND_DESIGN.md](06_SHEET_ANALYSIS_AND_DESIGN.md) | Разбор исходной книги: что откуда взялось |
| [docs/ТЗ_1С_запросы_GET_v10.xlsx](docs/ТЗ_1С_запросы_GET_v10.xlsx) | Спецификация GET-запросов 1С (от подрядчика) |
| [docs/nkt/API.md](docs/nkt/API.md) | НКТ: фактический контракт API, статусы заявок, расхождения с ТЗ |

---

## Структура

```
backend-go/
  cmd/server/      main.go — все маршруты /api/v1, CORS, раздача фронтенда
  internal/
    modules/       обработчики по доменам (orders, warehouse, finance, integration …)
    costing/ ordercosting/ warehouse/   себестоимость, FIFO-партии, резервы, подряд
    onec/ integration/ bitrix/ events/  клиент 1С, outbox/inbox, Б24, SSE
  scripts/dev.sh   локальный запуск с backend/.env
  Dockerfile       образ: фронтенд + бэкенд в одном контейнере
backend/
  prisma/          схема, миграции, сиды, импортёры 1С/Excel (lib/ — общие хелперы)
  backups/         дампы Postgres перед крупными импортами
  docker-compose.yml   postgres (всегда) + api (профиль prod)
  tools/           мок HTTP-сервисов 1С
frontend/src/
  pages/           Заказы, Спецификации, План, Цех, Склад, Финансы, Обмен с 1С
  components/      таблицы, статусы, виджеты дашборда
  hooks/ api/      react-query поверх axios
```

Тесты: `cd backend-go && go test ./...`. Сверка с прежним NestJS-бэкендом делалась запрос-в-запрос на живой базе; исходники Nest — в git-истории (stash «NestJS backend before removal»).

---

## Деплой: фронтенд на Vercel, бэкенд с базой на VPS

```
браузер ──https──▶ Vercel (статика, CDN)
   └────https──▶ https://api.<домен> ──▶ Caddy ──▶ Go :3000 ──▶ Postgres
                                        (VPS, всё внутри сети Docker)
```

База наружу не смотрит вообще, Go — только через Caddy. Нужен домен для API:
страница по https не может обращаться к `http://<ip>`, браузер это заблокирует.

**1. VPS.** A-запись `api.<домен>` → адрес сервера, открыты порты 80 и 443.

```bash
git pull
npm run install:all                         # один раз: зависимости для скриптов Prisma
cp backend/.env.example backend/.env        # см. таблицу ниже — заполнить обязательные
npm run db:up                               # postgres
npm --prefix backend run prisma:push        # схема БД (ведётся через db push, не миграциями)
npm --prefix backend run apply-views        # SQL-вью
npm run prod:up                             # сборка образа + api + caddy
curl https://api.<домен>/api/v1/health      # {"status":"ok","db":"ok"}
```

| Переменная в `backend/.env` | Зачем |
| --- | --- |
| `JWT_SECRET` | Обязательна: без неё контейнер не стартует (`REQUIRE_SECRETS=1`). Смена разлогинивает всех |
| `INTEGRATION_1C_SECRET` | Обязательна: подпись вебхуков 1С |
| `API_DOMAIN`, `LETSENCRYPT_EMAIL` | Домен API и почта для Let's Encrypt — Caddy выпустит сертификат сам |
| `CORS_ORIGINS` | Домен фронта на Vercel, через запятую. Пусто — разрешены все источники |
| `ONEC_BASE_URL`, `ONEC_LOGIN`, `ONEC_PASSWORD` | Адрес и учётка 1С |

**2. Vercel.** В настройках проекта — переменная окружения `VITE_API_URL` =
`https://api.<домен>`, затем передеплой. Сборка описана в `vercel.json`
(корень репозитория, вывод `frontend/dist`).

Обновление: `git pull`, при изменениях `schema.prisma` — `prisma:push` и
`apply-views`, затем `npm run prod:up`. Фронтенд обновляется отдельно пушем
в Vercel. Часовой пояс контейнера — `TZ=Asia/Almaty` (границы месяцев в
дашбордах считаются в локальном времени). Если менялись зависимости
фронтенда, обновите `frontend/package-lock.json`
(`npm --prefix frontend install --package-lock-only`).

**Тот же образ умеет работать и без Vercel:** он раздаёт собранный фронтенд
сам (`STATIC_DIR`), поэтому `https://api.<домен>/` открывает полный интерфейс —
запасной вход, если Vercel недоступен.

### Пилот в локальной сети завода (без домена и HTTPS)

Тот же образ на одном компьютере предприятия, адрес для людей —
`http://<адрес-компьютера>:3000`. Пошагово — [docs/ЗАПУСК.md](docs/ЗАПУСК.md).

```bash
npm run pilot                    # секреты в .env + postgres + api-pilot + ежедневные бэкапы
docker exec erp_api_pilot /app/smoke http://localhost:3000   # дымовой прогон: здоровье, права, ~80 ручек, раздача фронтенда
npm run db:backup <пометка>      # ручной дамп в backend/backups перед рискованным шагом
```

Профиль `pilot` открывает порт 3000 всей сети (трафик по http), поэтому
только внутри предприятия; для доступа снаружи — профиль `prod` выше.

## Проверить перед продом

- `JWT_SECRET`, `INTEGRATION_1C_SECRET`, пароль Postgres — сейчас dev-дефолты в коде и `docker-compose.yml`, задать через окружение (`backend/.env` читается контейнером `api`).
- `ONEC_BASE_URL` в `.env.example` указывает на мок (`localhost:8081`) — на сервере поставить адрес 1С.
- Пароль Postgres (`erp_password`) — дефолтный; менять его на уже созданном томе нужно через `ALTER USER`, переменная в compose на существующую базу не влияет.
- SQL-вью применяются сидом (`views.seeder.ts`), а не миграцией: `prisma migrate deploy` их не создаст.
